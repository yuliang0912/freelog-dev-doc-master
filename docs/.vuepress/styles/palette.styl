.page .theme-default-content:not(.custom) {
  max-width: 1080px;
}

.page table {
  th:first-of-type {
    max-width: 20%;
  }

  th:nth-of-type(2) {
    width: 100px;
  }

  th:nth-of-type(3) {
    width: 140px;
  }

  th:last-of-type {
    min-width: 600px;
  }
}
